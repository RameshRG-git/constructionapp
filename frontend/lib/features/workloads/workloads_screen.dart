import 'package:flutter/material.dart';

class WorkloadsScreen extends StatelessWidget {
  const WorkloadsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Workloads'));
  }
}
